# Studio 2.1.0 — archived APK

- Inline camera preview with persistent bandwidth-saving switch and fullscreen.
- Three fan sliders with coalesced command updates.
- Local buffering before timelapse playback; 120-second video unlock allowance.
- Embedded G-code thumbnail fallback for files missing history images.
- Home chamber temperature and print progress; compact job status above tabs.

This release preserves the original built APK. Its exact historical source snapshot was not committed during development and cannot be supplied. The tag records this archive entry, not buildable 2.1.0 source. GitHub's automatically generated source archives are therefore not the source for this APK. Use v2.1.1 for the published source baseline.

Read-only status, camera, file/history, thumbnail and video-transfer checks were performed against a physical CC1. No print was started. Android 7+, ARM32/ARM64; development-key signed.
