# Studio 2.0.0 — archived APK

First Studio redesign: German light/dark interface, readable filenames and folders, print options, detailed history, timelapse playback/download/export and improved SDCP request handling.

This release preserves the original built APK. Its exact historical source snapshot was not committed during development and cannot be supplied. The tag records this archive entry, not buildable 2.0.0 source. GitHub's automatically generated source archives are therefore not the source for this APK. Use v2.1.1 for the published source baseline.

Android 7+, ARM32/ARM64. Development-key signed. Install the attached APK, not the source archive. Tested with an emulator and simulated printer; this version was not hardware-verified.
